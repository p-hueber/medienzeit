# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  0x300000,
# The balance journal. Deliberately its own partition: keeping it inside the app
# image means a firmware that grows past its neighbour quietly eats the balance.
# Offset and size are mirrored in src/storage.rs.
storage,    data, fat,     0x310000, 0x4000,
